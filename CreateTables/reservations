CREATE TABLE reservations (
    id VARCHAR(MAX) NOT NULL,
    startTime DATETIME NOT NULL,
    endTime DATETIME ,
    createdAt DATETIME,
    updatedAt DATETIME,
    vehicleId VARCHAR(MAX) NOT NULL,
    parkingId VARCHAR(MAX) NOT NULL,
    clientId VARCHAR(MAX),

    CONSTRAINT reservations_pkey PRIMARY KEY CLUSTERED (id)
);
